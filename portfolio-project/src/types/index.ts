export interface Technology {
    id: number;
    name: string;
    icon: string;
}

export interface Projects {
    id: number;
    name: string;
    info: string;
    icon: string;
    code: string;
}